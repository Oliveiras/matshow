import{useState}from'react';import{LabTabs}from'../../../components/ui/LabTabs';import{ConversionActivity}from'./conversion/ConversionActivity';import{AreaPerimeterActivity}from'./area-perimeter/AreaPerimeterActivity';import{VolumeActivity}from'./volume/VolumeActivity'
type Tab='conversion'|'area'|'volume';const tabs=[{id:'conversion',label:'Estação de medidas',skills:'EF05MA19'},{id:'area',label:'Área × perímetro',skills:'EF05MA20'},{id:'volume',label:'Construtor de volume',skills:'EF05MA21'}]as const
export function MeasuresArea(){const[tab,setTab]=useState<Tab>('conversion');return <section className="lab-page"><LabTabs tabs={[...tabs]} active={tab} onChange={setTab}/>{tab==='conversion'&&<ConversionActivity/>}{tab==='area'&&<AreaPerimeterActivity/>}{tab==='volume'&&<VolumeActivity/>}</section>}
